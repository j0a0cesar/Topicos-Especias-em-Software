from classe import Classe

class Clerigo(Classe):
    def __init__(self):
        super().__init__()
        self.armadura = "pode usar todas as armaduras"
        self.armas = "só podem usar armas impactantes"
        self.intensMagigos = "o Clérigo é capaz de usar todos os tipos de itens mágicos desde que sejam ordeiros"
